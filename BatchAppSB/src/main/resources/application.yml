server:
  port: 8888
spring:
  batch:
    jdbc:
      initialize-schema: ALWAYS
  datasource:
    password: 17Bd5a0413@
    url: jdbc:mysql://localhost:3306/implementer
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
